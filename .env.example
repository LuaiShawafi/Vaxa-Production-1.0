# Copy to .env.local and fill in your LOCAL development values.
# Never commit real credentials. .env.local is git-ignored.

DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
